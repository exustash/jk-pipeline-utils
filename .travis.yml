

language: groovy

jobs:
  include:
    - stage: Build
      script: make deps.install
    - stage: Tests
      script: make static.lint
    - # tests
      script: make test.unit
    - stage: Release
      script: echo "Deploying to GitHub releases ..."

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
